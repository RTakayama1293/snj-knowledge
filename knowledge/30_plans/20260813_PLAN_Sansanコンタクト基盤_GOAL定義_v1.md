# 20260813_PLAN_Sansanコンタクト基盤_GOAL定義_v1

作成日: 2026-08-13（同日改訂: GitHub＋Claude Code完結型へ転換）
用途: Sansanコンタクトデータベース構築の `/goal` 入力原本（たたき台）
精度ゾーン: 70%（方針・設計判断。確定前のドラフト）
状態: **確定（2026-08-13）**。Q1〜Q3・Q5・Q6は決着済み。Q4・Q7は後段検討として§8に残置。2026-08-13同日、最終ゴールをDL→DWH型に改訂（§4）

---

## 0. 方針転換（2026-08-13・高山さん）

- ナレフルチャットのRAGで辻褄を合わせる設計をやめ、**GitHubリポジトリ＋Claude Codeで完結する構成に転換する**
- 理由: ナレフルのハック（参照ファイル手動差し替え・月次フラット蓄積・長CSV分割）への対処に設計工数を割く価値がない。Sansanは API、営業メールはフィルタリングで所定の場所へ送れるため、入口の自動化が可能
- **ナレフルチャットは「本基盤の再現用モック」と割り切る**。廃止はしない。案件整理モックv2は正本パイプラインの営業向け再現・デモ用として残し、正本の更新経路には含めない
- 同日の判断: 正本は新規専用リポジトリ／Sansan取得はGitHub Actions Secrets＋定期実行／照会窓口は当面高山さん経由

初版（ナレフル入口/出口案）とv3突合の経緯は§10を参照。

---

## 1. 背景

- Sansanコンタクトの蓄積が進み、CSV単位の都度処理から、検索・照会・分析に耐えるデータベースへの移行が必要になった
- 親文書「20260703_PLAN_情報統合基盤_RAGと構造化の全体設計_v3.md」は2026-08-13に回収済み（knowledge/30_plans/）。本書はそのL1（溜める）〜L2（処理）〜L3（正本）構造を、コンタクトという新規ストックについてGitHub＋Claude Codeで実装する位置づけ
- 入口の自動化余地: SansanはOpen API（契約プラン依存・Q1）、営業メールはGmailフィルタで所定ラベルへ集約できる

---

## 2. 確定している前提

| # | 前提 | 出所 |
|---|---|---|
| P1 | 名刺登録の判断基準は「Sansan名刺登録ガイドライン」が正。ランクA〜E・6ヶ月ルール | knowledge/10_rules/20250911 |
| P2 | 正本は**新規専用リポジトリ**（仮称snj-contacts・プライベート必須）。snj-knowledgeとは分離する | 2026-08-13 高山さん回答 |
| P3 | Sansan取得はAPI。キーは**GitHub Actions Secretsのみ**に置き、CCW環境変数・リポジトリには置かない | 2026-08-13 高山さん回答 |
| P4 | API契約可否（Q1）が未確認の間もCSVエクスポートで代替できる構成にする（取得層だけ差し替え可能に） | 設計判断 |
| P5 | 営業メールはフィルタリングで所定ラベル/アドレスへ集約し、Claude CodeがGmail MCPコネクタで読み取る（認証情報をどこにも置かない） | 2026-08-13 高山さん発言 |
| P6 | コンタクトには個人情報（氏名・所属・連絡先）が含まれる。外部サービスへのアップロード禁止・公開リポジトリ禁止 | セキュリティ規約 |
| P7 | 照会窓口は**当面高山さん経由**。営業が直接聞くUIは後段の検討（Q7） | 2026-08-13 高山さん回答 |
| P8 | ナレフルチャットは**再現用モック**。正本から生成した配布コピーで動かすデモ・検証用途に限定する | 2026-08-13 高山さん発言 |

---

## 3. 設計の骨格（たたき台）

```
Sansan ──Open API（Actions定期実行・キーはSecrets）──> 専用リポジトリ（正本DB）
                                                          ↑ 構造化コミット
営業メール ──Gmailフィルタ──> 所定ラベル ──Gmail MCP──> Claude Code（整理・名寄せ）

照会「○○社の誰に会った？」──> 高山さん ──> Claude Codeセッション ──> 出典付き回答

（ナレフルチャット＝上記の再現用モック。正本から配布コピーを渡して動かす。正本更新には関与しない）
```

正本・処理・照会をすべてGit＋Claude Codeに寄せることで、初版で問題だったナレフル側の制約（手動差し替え・月次フラット蓄積・長CSV劣化）が構造ごと消える。名寄せ・履歴・出典はGitの得意分野で解く。

この構成はv3のL1（溜める）→L2（処理）の実装であり、DL=data/raw/、DWH=data/processed/ が対応する。

---

## 4. 最終ゴール（1文）

> **営業接点の非構造化データ（Sansanコンタクト・営業メール）が欠損なくデータレイク（DL）として蓄積され、snj-contactsリポジトリ内のコードだけでDWH（名寄せ済み構造化データ）への変換が再現できる。「○○社の誰に会った？」に出典付きで答えられることは、このDWHの一利用である。**

| 層 | 実体 | 内容 |
|---|---|---|
| DL（データレイク） | `data/raw/` | Sansan生JSON・メール全文JSON。イミュータブル・全文保持（省略禁止） |
| DWH（データウェアハウス） | `data/processed/` | 名寄せ済み3CSV等。DLからスクリプトで再生成可能 |
| 変換 | `src/` | 再実行可能・冪等。スキーマ変更時はDLから再変換 |

---

## 5. 受け入れ基準

| # | 条件 | 検証方法 |
|---|---|---|
| G1 | 既存Sansanコンタクトの全件が正本DBに取り込まれ、重複（同一人物・同一社）が名寄せされている | 件数突合＋重複検出スクリプト |
| G2 | スキーマが文書化されている（安定ID・ランク・KWタグ・進捗メモ・出典） | SPEC文書の存在と実データとの一致 |
| G3 | 「○○社の誰に会った？」にClaude Codeセッションが出典付きで答えられる（高山さん経由運用で1サイクル） | 実照会の通し確認 |
| G4 | Sansan定期取得（Actions）が人手ゼロで回っている（API不可の場合: CSV手順が週次で回っている） | 2サイクル連続の自動実行ログ |
| G5 | 営業メールがフィルタ→取り込み→正本反映まで通っている | 1サイクル実運用 |
| G6 | 個人情報の線引き（照会出力・モック配布コピーに載せる項目）が定義され、守られている | 出力の項目監査 |
| G7 | ナレフルモックが正本から生成した配布コピーで再現できる（更新手順が文書化されている）※優先度低 | モックでの照会テスト |
| G8 | 非構造化データが全文・欠損なくDLに蓄積されている（メール本文の省略・要約保存の禁止） | rawファイルの抽出元との突合 |
| G9 | DWHがDLからリポジトリ内コードのみで再生成でき、再実行が冪等である | クリーン環境での再生成＋ハッシュ比較 |

---

## 6. スコープ

**含む**
- 専用リポジトリの設計（構成・アクセス権・CLAUDE.md）
- コンタクトDBのスキーマ設計（SPEC起草）
- Sansan取得ワークフロー（Actions定期実行。API不可時はCSV代替手順）
- 営業メール取り込みフロー（フィルタ設計＋Gmail MCP読み取り）
- 既存コンタクトの取り込み・名寄せ
- 照会運用（高山さん経由）の手順化

**含まない**
- 営業が直接照会するUI（Q7・後段）
- BU/SI一覧・パイプラインとの紐付け（Q4・後回し継続)
- SFA/CRM導入の検討
- ナレフルモックの高度化（再現に必要な範囲のみ維持）

---

## 7. 制約

| # | 制約 | 影響 |
|---|---|---|
| C1 | Sansan Open APIは契約プラン依存。**可否未確認** | 最優先で確認（Q1）。不可ならCSVエクスポート経路で開始し、取得層のみ後日差し替え |
| C2 | CCWの環境変数は環境利用者全員が読める | APIキーはGitHub Actions Secretsのみ。CCWセッションからAPIを直接叩かない |
| C3 | 専用リポジトリはプライベート必須（P6） | 配布コピー（モック用含む）を作る場合はQ5の線引きを先に確定 |
| C4 | メールフィルタの設定はGmail側の管理権限に依存 | フィルタ仕様（Q6）を情シス/管理者と握る |
| C5 | ランク定義（A〜E）はガイドライン準拠だが見直されうる | スキーマ・スクリプトにハードコードしない |

---

## 8. 未決事項

| # | 論点 | 判断が必要な理由 |
|---|---|---|
| Q1 | **Sansan Open APIの契約可否** | **解決（2026-08-13）**: API利用可・実機取得成功 |
| Q2 | 専用リポジトリの名称とアクセス権設計 | **解決**: snj-contacts作成済み |
| Q3 | 名寄せの単位とキー設計（人単位か会社単位か。SansanのIDを安定キーに使えるか） | **解決**: 会社単位・company_id内部採番（スキーマSPEC参照） |
| Q4 | BU/SI・パイプラインとの連携時期 | 初期構築には含めない（2026-08-13回答）。着手時期のみ未決 |
| Q5 | 照会出力・モック配布コピーに載せる個人情報の線引き | **解決**: 連絡先は正本のみ（スキーマSPEC§5参照） |
| Q6 | メールフィルタの仕様（対象アドレス・ラベル体系・転送方式） | **解決**: 本文ベースフィルタ＋ラベル運用で稼働確認済み（メール取込SPEC参照） |
| Q7 | 営業直接照会UIの要否と形 | 当面高山さん経由（P7）。運用が回ってから再検討 |

---

## 9. 直近のアクション

| # | 内容 | 担当 | 依存 |
|---|---|---|---|
| A1 | Sansan Open APIの契約可否を確認 | 高山 | **完了**（API確認済み） |
| A2 | 専用リポジトリの作成（名称・権限はQ2確定後） | 高山 | **完了**（snj-contacts作成・初期構成PR #1マージ） |
| A3 | SansanエクスポートCSVのサンプル1本の配置（スキーマ実測用。API可否に関わらず必要） | 高山 | **完了**（初回取得31件で代替） |
| A4 | コンタクトDBスキーマ仕様書（SPEC）の起草 | ミナト | **完了**（スキーマSPEC起草済み） |
| A5 | Sansan取得ワークフロー（Actions）のSPEC起草 | ミナト | **完了**（取得ワークフロー稼働・SPEC v1） |
| A6 | メール取り込みフローのSPEC起草（フィルタ仕様Q6の たたき台含む） | ミナト | **完了**（メール取込SPEC起草済み） |

次フェーズのタスク管理はsnj-contactsリポジトリ側で行う。

---

## 10. 経緯: ナレフル入口/出口案からの転換

- 初版（2026-08-13午前）は「正本=snj-knowledge、ナレフル=入口（整理）と出口（照会）の窓口」の二層構成だった。親文書v3との突合結果（整合5点・差分3点）を含む初版全文はGit履歴（コミット 0957682〜60eb99f）を参照
- 同日、高山さんの判断で本書§0の構成に転換。初版の制約C1〜C3（ナレフルの手動差し替え・月次フラット蓄積・長CSV劣化）とG5（ナレフルのハック可能範囲検証）は、対処すべき問題ごと消滅したため削除
- v3との関係: v3の「ナレフルチャット=L2処理層」は、本基盤では「再現用モック」に位置づけを変える。v3自体の改訂（3章正本表へのコンタクトDB追加、L2の位置づけ変更）は本書確定時に起票する

---

**関連**:
- 20260703_PLAN_情報統合基盤_RAGと構造化の全体設計_v3.md（親文書。L1/L2/L3構造の出典）
- 20260703_SPEC_ナレフルエージェント_案件整理モック構築ガイド_v2.md（再現用モックとして存続）
- 20250911_DOC_営業戦略_Sansan名刺登録ガイドライン.md（登録基準・ランク定義）
- snj-contacts docs/20260813_SPEC_snj-contacts_コンタクトDBスキーマ_v1.md（名寄せ設計・個人情報の線引き）
- snj-contacts docs/20260813_SPEC_snj-contacts_メール取込フロー_v1.md（メールフィルタ仕様）

**作成者**: 高山 + ミナト
